 <div class="col s12 m7">
    <h2 class="header"> {{question}} </h2>
    <div class="card horizontal">
      <div class="card-stacked">
        <div class="card-content center">
            <div class="row">
              {{#each answer}}
          <button class="waves-effect waves-light btn">{{this}}</button>
            {{/each}}
          </div>
        </div>
      </div>
    </div>
  </div>

